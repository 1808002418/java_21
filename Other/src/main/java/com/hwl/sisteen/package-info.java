package com.hwl.sisteen;
// java16特性
/**
 * 1. 包装类构造方法的警告,改为使用静态方法. 比如 Integer.valueOf(1)
 * 2. 不建议使用包装类作为锁对象，倘若使用包装类作为锁对象，在编译时会出现警告。
 * 3. InvocationHandler新增方法. 代理用的, 新增一个方法可以调用父接口中defalut方法
 * 4. 新增日时段 可以获取现在时间对应的日时段，上午，下午等
 */